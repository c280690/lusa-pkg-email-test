'use strict';

const { assert } = require('chai');

const index = require('./');

describe('finalize-html() method', () => {

	it('is a function', () => {
		assert.isFunction(index);
	})

})